# reverse_one_linked_list
Reverse the one linked list using C++


## Run application

* clone this repository
* 
$ cd cppipe # local repository
$ mkdir build
$ cd build
$ cmake ..
$ cmake --build .
$ ctest -C Debug # run tests if needed
$ sudo cmake --install .
